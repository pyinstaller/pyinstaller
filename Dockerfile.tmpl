FROM python:${PY_VERSION}

ENV TOOL_DEPS \
    mercurial

ENV LIBRARY_DEPS \
    gfortran \
    libatlas-base-dev \
    libdbus-glib-1-dev

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        $TOOL_DEPS \
        $LIBRARY_DEPS

WORKDIR /opt/pyinstaller

COPY . .
